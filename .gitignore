# macOS
.DS_Store

# Claude Code local session/config
.claude/

# Local-only project guide for Claude — never publish to the open-source repo
CLAUDE.md

# Secrets — never commit these
.env
*.local

# Transient Playwright MCP artifacts
.playwright-mcp/
